#ifndef HEAP_SORT_H
#define HEAP_SORT_H

#include <vector>

namespace heap_sort {

class HeapSort {
public:
    static void sort(std::vector<int>& values);
};

} // namespace heap_sort

#endif // HEAP_SORT_H
